import Handler from "./modules/Handler.js";

class AdataFlowSSR {
    constructor(options = {}) {
        this.options = options;
        this.handlers = [];
    }

    process(request) {

    }
}

export default AdataFlowSSR