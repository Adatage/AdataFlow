class AdataSocket {
    static protocol = Object.freeze({
        WEBSOCKET: 0,
        WEBTRANSPORT: 1,
        WEBRTC: 2
    });

    constructor(uri, protocol) {
        
    }
}

export default AdataSocket
